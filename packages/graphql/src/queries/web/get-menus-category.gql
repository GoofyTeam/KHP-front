query GetMenuCategories($first: Int, $page: Int) {
  menuCategories(first: $first, page: $page) {
    data {
      ...MenuCategoryFields
    }
    paginatorInfo {
      ...PaginatorInfoFields
    }
  }
}
