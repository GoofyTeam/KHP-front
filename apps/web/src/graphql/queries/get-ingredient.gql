query GetIngredient($id: ID!) {
  ingredient(id: $id) {
    id
    name
    unit
    image_url
    quantities {
      quantity
      location {
        id
        name
        locationType {
          name
        }
      }
    }
    category {
      id
      name
    }
    stockMovements {
      id
      type
      quantity
      quantity_before
      quantity_after
      created_at
      location {
        id
        name
      }
      user {
        id
        name
      }
    }
  }
}
