query openFoodFactsProxy($barcode: String, $page: Int!) {
  search(barcode: $barcode, page: $page) {
    barcode
    base_quantity
    imageUrl
    product_name
    categories
    unit
    is_already_in_database
    ingredient_id
  }
}
