query getIngredient($id: ID, $barcode: String) {
  ingredient(id: $id, barcode: $barcode) {
    id
    image_url
    name
    categories {
      name
      id
    }
    unit
    base_quantity
    quantities {
      location {
        id
        name
        locationType {
          name
        }
      }
      quantity
    }
  }
}
