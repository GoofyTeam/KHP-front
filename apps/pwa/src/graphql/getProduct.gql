query GetIngredient($id: ID!) {
  ingredient(id: $id) {
    id
    name
    unit
    image_url
    categories {
      name
    }
    quantities {
      quantity
      location {
        id
        name
      }
    }
  }
}
